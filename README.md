# Initial Setup of Database

To run the scripts and notebooks in `initial_setup/`, you need:

1. **Python 3.12**  
2. Install the optional dependencies for setup (including Refinitiv) via Poetry:

```bash
poetry install --with setup,refinitiv
```
