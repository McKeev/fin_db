# .SQL Sandbox

Use the `sandbox.sql` file to test SQL queries/migrations.
You can then use `run.sh` file to conveniently run this file!
By default, it uses the migrator role, so be careful future me :)